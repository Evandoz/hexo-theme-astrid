<% if (theme.social){ %>
  <div class="widget widget_social">
    <ul class="social-menu-widget clearfix">
      <% Object.keys(theme.social).forEach(function(key) { %>
        <li><a href="<%= theme.social[key] %>" title="<%=key.trim()%>"></a></li>
      <% }) %>
    </ul>
  </div>
<% } %>
